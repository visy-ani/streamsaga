datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator clien { 
  provider = "prisma-client-js"
}

model User = {
  id       String   @id @default(uuid())
  name     String? 
  email    String   @unique 
  password String
}